# On Windows & Mac

## cd into the backend folder

### Create a python virtual environment (make sure python is updated 3.13.x)

### Windows => python -m venv venv => .\venv\Scripts\Activate.ps1 => pip install -r requirements.txt

### Mac / Linux => python3 -m venv venv => source venv/bin/activate => pip install -r requirements.txt

## make sure you add into your .gitignore to not push the venv and .env files!!!! I can help you set it up when you need just text me
